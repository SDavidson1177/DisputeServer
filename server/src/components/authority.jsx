// UI for the authority that is backing the vendors
// This authority must attest to the validity of the vendor's
// license.
export function AuthorityUI() {
    return(
        <>
            <h1>Authority UI</h1>
            
        </>
    );
}